# Part 2 — Analysis JSON → Interactive HTML

This Streamlit app accepts one or many bank analysis JSON files (schema v5.x / v4.0) and generates interactive HTML reports.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy on Streamlit Community Cloud
- Repository contains: app.py, requirements.txt
- Main file path: app.py
